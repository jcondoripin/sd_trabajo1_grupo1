Para ejecutar este proyecto de manera segura create un entorno virtual, si no le sabes aqui te dejo los comando:

```
python -m pip venv env
.\env\Scripts\activate # para windows
python -m pip install -r requirements.txt # instalar dependencias
python app.py
```
